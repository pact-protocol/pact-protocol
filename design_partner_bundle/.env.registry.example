# Copy to repo root as .env.registry and fill in keys.
# Generate keys: pnpm -C packages/registry gen:keys
# Used only for live issuance demos; NOT required for offline verification or viewer demo.

REGISTRY_ISSUER_PUBLIC_KEY_B58=
REGISTRY_ISSUER_SECRET_KEY_B58=

# Optional: API key for issue/revoke (default: dev-api-key)
# REGISTRY_API_KEY=dev-api-key

# Optional: data directory (default: packages/registry/data)
# REGISTRY_DATA_DIR=
