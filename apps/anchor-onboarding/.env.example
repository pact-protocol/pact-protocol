# Anchor Onboarding — env vars
# Copy to .env and customize for your deployment.

# ===== Required for issue/revoke (proxies to registry) =====
REGISTRY_URL=http://localhost:3100
REGISTRY_API_KEY=dev-api-key

# ===== Optional: Production integrations =====

# Stripe Connect OAuth (platform_verified anchors with real Stripe account)
# Get from https://dashboard.stripe.com/settings/connect
# When set, "Connect with Stripe" flow issues anchors with real account_id_fingerprint.
# STRIPE_CLIENT_ID=ca_...
# STRIPE_CLIENT_SECRET=sk_...
# STRIPE_CONNECT_REDIRECT_URI=http://localhost:3043/api/stripe/callback

# OIDC verification (oidc_verified anchors with real id_token)
# Issuer URL for JWT validation (e.g. https://acme.okta.com)
# When set, POST /api/oidc/verify accepts id_token and returns assertion_fingerprint.
# OIDC_ISSUER=https://issuer.example.com
# OIDC_CLIENT_ID=...
# OIDC_JWKS_URI=https://issuer.example.com/.well-known/jwks.json

# ===== Server =====
ONBOARDING_SERVER_PORT=3043
