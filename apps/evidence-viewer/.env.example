# Evidence Viewer — optional env vars
# Copy to .env and customize for your deployment.

# Base path when deployed to a subpath (e.g. /evidence-viewer/). Omit for root deployment.
# VITE_BASE_PATH=/

# Anchor onboarding URL for "Manage anchors" / "Anchor onboarding" links in the Party modal.
# MUST point to the Anchor Onboarding app, NOT the Evidence Viewer (5173).
# Common ports: 5175 (default) or 5176 if 5175 is in use. Use whatever port the onboarding app prints.
# Examples: http://localhost:5175, http://localhost:5176, or https://onboarding.example.com (production)
# In dev, defaults to http://localhost:5175 if unset.
VITE_ANCHOR_ONBOARDING_URL=
